# 기본 설정 (사용자 수정용)
# Default settings (for user modification)

# 언어 설정 (local: 시스템 로케일 사용, ko: 한국어, en: 영어)
# Language settings (local: use system locale, ko: Korean, en: English)
# - local: 시스템 로케일 사용 (한국어: Asia/Seoul, 영어: UTC)
# - local: use system locale (Korean: Asia/Seoul, English: UTC)
# - ko: 한국어 (Asia/Seoul)
# - ko: Korean (Asia/Seoul)
# - en: 영어 (UTC)
# - en: English (UTC)
LANGUAGE=local

# 로케일 설정 (설치 과정에서 자동 설정됨)
# Locale settings (automatically set during installation)
LOCALE=

# 타임존 설정 (설치 과정에서 자동 설정됨)
# Timezone settings (automatically set during installation)
TIMEZONE=

# 기본 비밀번호
# Default password
PASSWORD=1234

# 작업 디렉토리 경로
# Working directory path
WORKDIR=work/project

# 베이스 이미지 (설치 과정에서 언어에 맞게 자동 설정됨)
# Base image (automatically set based on language during installation)
# - ko: namugach/ubuntu-basic:24.04-kor-deno
# - en: ubuntu:24.04
BASE_IMAGE=

# 디버그 모드 (true/false)
# Debug mode (true/false)
DEBUG=false 